# ignore all .txt files
# *.txt
cmake-build-*/
# slash at the end makes it a folder
.idea/
/git-filter-repo.py
.cache/
build/
.DS_Store
.claude/
CLAUDE.md

# Real relay endpoint + pinned cert fingerprint. Never committed.
# Template lives at source/relay_config.example.h.
source/relay_config.h
relay/cert.pem
relay/key.pem

